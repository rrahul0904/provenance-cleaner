export default function MissionPage() {
  return <main className="shell"><header className="section-heading"><div><p className="eyebrow">Mission</p><h1>Make content hygiene inspectable instead of mystical.</h1><p>Provenance Cleaner is built around a simple rule: deterministic evidence should be labeled as deterministic evidence, and probabilistic editing should never be sold as proof of authorship or detector evasion.</p></div><a href="/">Open workbench</a></header><section className="principles"><div><span>Local first</span><h3>Keep simple work simple</h3><p>Unicode scanning and supported file inspection/sanitation stay in the browser whenever practical.</p></div><div><span>Integrity</span><h3>Protect signed provenance</h3><p>Cryptographically bound provenance is not silently stripped or represented as still valid after modification.</p></div><div><span>Accountable AI</span><h3>Charge after validation</h3><p>Semantic edits reserve credits first and commit the debit only after deterministic preservation checks pass.</p></div></section></main>;
}

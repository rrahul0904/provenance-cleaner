import Link from "next/link";

export default function HowItWorksPage() {
  return <main className="shell"><header className="section-heading"><div><p className="eyebrow">How it works</p><h1>Three layers, three different verification standards.</h1><p>Provenance Cleaner separates deterministic byte/text checks from optional generative editing so a receipt never overstates what was actually proven.</p></div><Link href="/">Open workbench</Link></header><section className="principles"><div><span>01</span><h3>Scan</h3><p>Free local text scanning identifies tracked hidden Unicode characters and their positions without creating an account or debiting credits.</p></div><div><span>02</span><h3>Inspect files</h3><p>DOCX, PNG and JPEG metadata is classified before any sanitation. Signed provenance is surfaced separately because changing bound bytes can invalidate it.</p></div><div><span>03</span><h3>Edit carefully</h3><p>Optional semantic editing reserves credits, protects facts and references, calls Mistral through Vercel AI Gateway, validates the output deterministically, then commits or releases the hold.</p></div></section><section className="panel"><h2>Receipts show evidence, not magic</h2><p>Unicode cleaning rescans its output. File sanitation records before/after findings and hashes. Semantic editing reports protected-span preservation, length retention and overlap metrics. None of those measurements is presented as guaranteed AI-detector evasion.</p></section></main>;
}

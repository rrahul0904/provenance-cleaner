# Optional model override. Defaults to the current verified Mistral editing model.
TRANSFORM_MODEL=mistral/mistral-medium-3.5

# Local/non-Vercel fallback for AI Gateway authentication.
# Prefer Vercel OIDC after linking the project instead of committing or sharing a long-lived key.
AI_GATEWAY_API_KEY=

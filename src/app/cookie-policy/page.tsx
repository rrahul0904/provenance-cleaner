import { PageHero } from "@/components/quiet-forensics";
const STORAGE=[
["Authentication","Supabase session cookies maintain anonymous or authenticated sessions. Sensitive and billable API requests validate the current user with the Auth service rather than trusting only a locally valid token."],
["Guest identity","A guest session is designed to survive normal navigation and can be upgraded to a real account. Clearing browser/session state can make the previous guest identity unavailable from that browser; the product does not promise fingerprint recovery."],
["Bot protection","Cloudflare Turnstile may use data necessary to verify human interaction on account creation, Checkout and semantic editing. Production credentials are separate from Preview test credentials."],
["Analytics","Product analytics is currently disabled. No PostHog runtime is configured; any future analytics activation must be cookieless/memoryless, exclude submitted content and filenames, and emit zero events when Do Not Track is enabled."],
] as const;
export default function CookiePolicyPage(){return <main id="main-content"><PageHero eyebrow="Cookies & browser storage" title="Use storage for sessions, not submitted content." copy="Browser storage exists to preserve authentication and security state—not to become a hidden archive of the documents you inspect." aside="The latest verification receipt drawer is memory-only and disappears with the page session; it does not write raw content into localStorage or IndexedDB."/><section className="shell legal-grid">{STORAGE.map(([title,copy],index)=><article className="legal-card" key={title}><span className="mono-label">{String(index+1).padStart(2,"0")}</span><h2>{title}</h2><p>{copy}</p></article>)}</section></main>}

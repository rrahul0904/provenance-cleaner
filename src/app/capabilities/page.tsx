import Link from "next/link";
import { MAX_FILE_BYTES, MAX_REWRITE_WORDS } from "@/lib/product-contract";

export default function CapabilitiesPage() {
  return <main className="shell"><header className="section-heading"><div><p className="eyebrow">Capabilities</p><h1>Use the right tool for each provenance layer.</h1><p>Input routing is explicit so a file-only metadata job is not silently sent through a language model.</p></div><Link href="/">Back to workbench</Link></header><section className="panel"><h2>Paste or .txt</h2><p>Free Unicode scan and deterministic cleaning, plus optional semantic editing. A semantic edit accepts at most {MAX_REWRITE_WORDS.toLocaleString()} source words per operation and is billed by source word count.</p></section><section className="panel"><h2>DOCX</h2><p>Hidden-character and Office metadata inspection/sanitation. The reference parity route does not automatically rewrite Word-document prose. DOCX cleaning jobs are capped at {(MAX_FILE_BYTES / (1024 * 1024)).toFixed(1)} MB.</p></section><section className="panel"><h2>PNG and JPEG</h2><p>Privacy/generator metadata inspection and safe sanitation. Signed provenance indicators remain inspection-first rather than being silently removed.</p></section><section className="panel"><h2>Advanced inspection</h2><p>The pre-existing Provenance Cleaner PDF/WebP inspection path remains available as an additional capability even though it is not part of the reference parity contract.</p></section></main>;
}

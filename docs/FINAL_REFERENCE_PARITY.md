# Final Reference Parity

Date: 2026-09-21

Repository: `rrahul0904/provenance-cleaner`

Base `main` SHA for this completion wave: `615067d5e44f1fc9c2a54381d4ad8b7f2576fa77`

Completion branch: `unclaude-parity-finalization-20260921`

## Result

The complete previously captured Un-Claude clean-room contract is resolved end to end in Provenance Cleaner.

- Total known behaviors: **35**
- VERIFIED: **29**
- INTENTIONAL_DIVERGENCE: **5**
- UNVERIFIABLE_PUBLICLY: **1**
- Known implementation gaps: **0**

The single `UNVERIFIABLE_PUBLICLY` item is the exact current reference duration for guest-browser persistence. The public reference page is not directly reachable from the verification environment, so that precise competitor session-duration detail is not guessed. Provenance Cleaner's guest/session behavior is implemented and covered independently.

## Three-layer capability closure

### 1. Invisible Unicode

Implemented with exact code-point/position findings, conservative safe removal, explicit review states for language/emoji-sensitive controls, post-clean rescan and receipts.

### 2. File metadata and provenance

Implemented for DOCX/PNG/JPEG sanitation with reinspection before billing commit. PDF/WebP remain explicitly inspection-only in the public workbench. C2PA/Content Credentials are verified/identified and destructive mutation is blocked when provenance may be cryptographically bound.

### 3. Statistical text watermark

Implemented as substantive semantics-preserving parity rewriting with protected URLs, emails, quotes, citations, dates, signed numerics, currencies, percentages and reliable entities. Parity mode enforces approximately 90–110% output length and a maximum three-word unprotected shared run.

A `TextWatermarkVerifier` abstraction exists, but the default verifier reports `unavailable`. This is intentional: without legitimate access to the reference provider's detector/key, Provenance Cleaner does not claim that statistical watermark removal has been independently verified.

## Beyond the reference inventory

Provenance Cleaner additionally includes:

- official C2PA browser verification and signed-provenance safety;
- self-verifying SHA-256 artifact receipts/version chains;
- local/offline inspection shell;
- developer API keys with hashed secrets and revocation;
- machine-to-machine scan/transform/usage APIs;
- zero-dependency Node CLI;
- MV3 browser extension;
- append-only FIFO credits/reservations/refunds;
- recurring TEST-mode subscriptions/Billing Portal;
- account deletion reconciliation;
- Admin RBAC, FinOps and operational rollups;
- Turnstile, rate limiting, privacy-safe logs;
- exact-source deployment integrity and scheduled production verification.

## Release evidence

Required evidence remains additive:

1. unit tests, lint and production build;
2. Playwright browser suite;
3. release/source-manifest integrity;
4. Supabase readiness/security checks;
5. live health/readiness;
6. exact source/SHA production verification;
7. no newly observed public-reference behavior left unclassified.

The release may not claim detector-bypass, “undetectable,” or verified statistical-watermark removal without actual detector evidence.

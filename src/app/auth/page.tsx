import { AuthForm } from "@/components/auth-form";
import { PageHero } from "@/components/quiet-forensics";
export default function AuthPage(){return <main id="main-content"><PageHero eyebrow="Provenance Cleaner account" title="Keep credits and history across devices." copy="Create an account with email/password or Google. If this browser already has a guest identity, the upgrade flow preserves that same user, credit balance, and audit history." aside="A first-time verified identity can claim three signup credits once. Ordinary sign-in does not claim the promotion."/><div className="shell auth-shell"><AuthForm/></div></main>}
